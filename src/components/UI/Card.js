import "./Card.css";
import TextAreaFrame from "../text/TextAreaFrame.js";

const Card = (props) => {

  return (
    <div className="card">
      <TextAreaFrame />
    </div>
  );
};

export default Card;
